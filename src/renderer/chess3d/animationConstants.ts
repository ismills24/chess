// Baseline animation duration constants
// These serve as initial defaults for the runtime-configurable animation system
// Runtime values can be overridden via animationConfig setters (e.g., from Settings UI)

export const DEFAULT_MOVE_DURATION_MS = 400;
export const KNIGHT_MOVE_DURATION_MS = 500;
export const ANIMATION_BUFFER_MS = 100;
